<?xml version="1.0" encoding="utf-8"?>
<androidx.drawerlayout.widget.DrawerLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/drawerLayout"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- ═══ MAIN CONTENT ═══ -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:background="#EFEFEF">

        <!-- Navbar -->
        <include
            android:id="@+id/navbar"
            layout="@layout/view_navbar" />

        <!-- Scrollable body -->
        <ScrollView
            android:layout_width="match_parent"
            android:layout_height="match_parent">

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:padding="16dp">

                <!-- ── TAB BUTTONS ── -->
                <LinearLayout
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:orientation="vertical"
                    android:layout_marginBottom="16dp">

                    <!-- Row 1: Customers + Drivers -->
                    <LinearLayout
                        android:layout_width="match_parent"
                        android:layout_height="wrap_content"
                        android:orientation="horizontal"
                        android:layout_marginBottom="8dp">

                        <com.google.android.material.button.MaterialButton
                            android:id="@+id/btnTabCustomers"
                            android:layout_width="0dp"
                            android:layout_height="44dp"
                            android:layout_weight="1"
                            android:text="Customers"
                            android:textAllCaps="false"
                            android:textSize="13sp"
                            android:layout_marginEnd="8dp"
                            app:cornerRadius="8dp"
                            style="@style/Widget.MaterialComponents.Button" />

                        <com.google.android.material.button.MaterialButton
                            android:id="@+id/btnTabDrivers"
                            android:layout_width="0dp"
                            android:layout_height="44dp"
                            android:layout_weight="1"
                            android:text="Drivers"
                            android:textAllCaps="false"
                            android:textSize="13sp"
                            app:cornerRadius="8dp"
                            style="@style/Widget.MaterialComponents.Button" />

                    </LinearLayout>

                    <!-- Row 2: Profile Change Requests (full width) -->
                    <com.google.android.material.button.MaterialButton
                        android:id="@+id/btnTabRequests"
                        android:layout_width="match_parent"
                        android:layout_height="44dp"
                        android:text="Profile Change Requests"
                        android:textAllCaps="false"
                        android:textSize="13sp"
                        app:cornerRadius="8dp"
                        style="@style/Widget.MaterialComponents.Button" />

                </LinearLayout>

                <!-- ── SECTION TITLE ── -->
                <TextView
                    android:id="@+id/tvSectionTitle"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="All customers"
                    android:textSize="24sp"
                    android:textStyle="bold"
                    android:textColor="#3d4f47"
                    android:layout_marginBottom="4dp" />

                <!-- ── DRIVER EXTRAS (register link) ── -->
                <LinearLayout
                    android:id="@+id/layoutDriverExtras"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:orientation="vertical"
                    android:visibility="gone">

                    <TextView
                        android:id="@+id/tvRegisterDriver"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:text="Register a new driver"
                        android:textSize="14sp"
                        android:textColor="#0A0F2C"
                        android:textStyle="bold"
                        android:clickable="true"
                        android:focusable="true"
                        android:layout_marginBottom="10dp" />

                </LinearLayout>

                <!-- ── SEARCH (drivers only) ── -->
                <LinearLayout
                    android:id="@+id/searchWrap"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:orientation="horizontal"
                    android:gravity="center_vertical"
                    android:visibility="gone"
                    android:layout_marginBottom="8dp">

                    <EditText
                        android:id="@+id/etSearch"
                        android:layout_width="0dp"
                        android:layout_height="48dp"
                        android:layout_weight="1"
                        android:hint="Search driver by name..."
                        android:background="@drawable/bg_input"
                        android:padding="12dp"
                        android:inputType="text"
                        android:imeOptions="actionSearch" />

                </LinearLayout>

                <!-- ── USER LIST ── -->
                <androidx.recyclerview.widget.RecyclerView
                    android:id="@+id/rvUsers"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:nestedScrollingEnabled="false"
                    android:clipToPadding="false" />

            </LinearLayout>
        </ScrollView>

    </LinearLayout>

    <!-- ═══ DRAWER ═══ -->
    <com.google.android.material.navigation.NavigationView
        android:id="@+id/navView"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_gravity="start"
        android:background="#0A0F2C"
        app:menu="@menu/loged_drawer_menu"
        app:itemTextColor="@android:color/white"
        app:itemBackground="@android:color/transparent" />

</androidx.drawerlayout.widget.DrawerLayout>